const selectDark = {
  control: (styles) => ({}),
  option: (styles, { data, isDisabled, isFocused, isSelected }) => {},
};

const selectLight = {
  control: (styles) => ({}),
  option: (styles, { data, isDisabled, isFocused, isSelected }) => {},
};
